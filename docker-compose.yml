services:
  dynamodb-local:
    image: amazon/dynamodb-local:3.3.0
    command: "-jar DynamoDBLocal.jar -sharedDb -dbPath ./data"
    ports:
      - "8001:8000"
    volumes:
      - ./infrastructure/docker/dynamodb:/home/dynamodblocal/data
    working_dir: /home/dynamodblocal
