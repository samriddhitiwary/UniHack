# CatalogIQ Web

React JavaScript application built with Vite and Material UI.

```powershell
npm --prefix=apps/web install
npm --prefix=apps/web run dev
```

Use `npm --prefix=apps/web test -- --run`, `npm --prefix=apps/web run lint`, and `npm --prefix=apps/web run build` for checks.
